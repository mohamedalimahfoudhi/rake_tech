framework:
    asset_mapper:
        # The paths to make available to the asset mapper.
        paths:
            - assets/
        enabled: true
        missing_import_mode: strict
        excluded_patterns:
            - '**/*.map'
            - '*/assets/build/*'
        

when@prod:
    framework:
        asset_mapper:
            missing_import_mode: warn
